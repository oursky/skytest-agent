apiVersion: v1
kind: Service
metadata:
  name: skytest-control-plane
spec:
  type: ClusterIP
  selector:
    app: skytest-control-plane
  ports:
    - name: http
      port: 80
      targetPort: 3000
